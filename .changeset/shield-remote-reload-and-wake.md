---
'@provablehq/aleo-wallet-adapter-shield': patch
---

Remote (relay) sessions: a page reload restores the connection instead of waiting on the Shield app, and requests that need approval open the app on mobile browsers.
