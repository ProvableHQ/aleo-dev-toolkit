---
'@provablehq/aleo-wallet-adapter-shield': patch
---

Stamp `dapp.sameDevice` on relay `connect()` from the page user agent so the wallet can tell same-device deeplink pairing from a desktop QR session. Dapps do not configure the flag.
